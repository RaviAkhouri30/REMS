import { LoginCredentialModel } from './login-credential-model';

describe('LoginCredentialModel', () => {
  it('should create an instance', () => {
    expect(new LoginCredentialModel()).toBeTruthy();
  });
});
